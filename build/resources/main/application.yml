spring:
  datasource:
    # mysql: com.mysql.cj.jdbc.Driver
    # mariadb: org.mariadb.jdbc.Driver
    driver-class-name: com.mysql.cj.jdbc.Driver
    # mysql: jdbc:mysql://localhost:3306/testdb
    # mariadb: jdbc:mariadb://localhost:3306/testdb
    url: jdbc:mysql://preprod.cd6adngdknme.ap-northeast-2.rds.amazonaws.com:3306/globuddy
    username: admin
    password: 78lnBOkmNwyk36NQyENu
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    show-sql: true


cloud:
  aws:
    s3:
      bucket: glob-min
    credentials:
      access-key:
      secret-key:
    region:
      static: ap-northeast-2
      auto: false
    stack:
      auto: false